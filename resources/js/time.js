import axios from "axios";
var moment = require("moment"); // require

window.onload = function () {
    openTime = document.getElementById("openingTime").innerText;
    closeTime = document.getElementById("closingTime").innerText;
};
